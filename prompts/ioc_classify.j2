你是一个威胁情报分析专家。请分析以下 IOC（威胁指标）是否与恶意活动相关。

## 分析原则
1. 结合上下文判断：IOC 是否出现在攻击描述、入侵事件等安全上下文中
2. 如果 IOC 出现在"参考链接"、"致谢"、"附录"等非攻击性章节，应判为非恶意
3. 如果 IOC 明确被描述为攻击者使用的 C2、恶意域名、钓鱼 URL 等，应判为恶意
4. 不确定时标为 suspicious

## 输出格式
请按 JSON 数组格式返回，每个元素包含：
- value: IOC 值
- type: IOC 类型（ipv4/domain/url/md5/sha1/sha256）
- malicious: "malicious" | "suspicious" | "benign"
- label: 简短标签（如"C2服务器"、"钓鱼网站"、"恶意文件"、"合法服务"、"参考链接"、"CDN节点"、"待验证"等）
- confidence: 0-100 的置信度分数
- reason: 判断理由（中文）

## IOC 列表
{% for ioc in iocs %}
{{ loop.index }}. [{{ ioc.type }}] {{ ioc.value }}
   上下文: "{{ ioc.context[:200] }}"
{% endfor %}

请只返回 JSON 数组，不要其他内容。
